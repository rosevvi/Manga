package com.manga.common.enums;

/** 定义项目内容可见范围，预留后续协作与公开展示能力。 */
public enum ProjectVisibilityScope {
    /** 项目公开可见。 */
    PUBLIC,
    /** 项目仅所有者可见。 */
    PRIVATE,
    /** 项目协作成员可见。 */
    TEAM
}
