<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manga.mapper.AgentConversationMapper">
    <select id="findOwned" resultType="com.manga.entity.AgentConversation">
        SELECT id, conversation_id, user_id, project_id, title, status, created_at, updated_at
        FROM manga_agent_conversation
        WHERE conversation_id = #{conversationId} AND user_id = #{userId}
    </select>
    <select id="findByUser" resultType="com.manga.entity.AgentConversation">
        SELECT id, conversation_id, user_id, project_id, title, status, created_at, updated_at
        FROM manga_agent_conversation
        WHERE user_id = #{userId}
        <if test="projectId != null">AND project_id = #{projectId}</if>
        ORDER BY updated_at DESC, id DESC
    </select>
</mapper>
