<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manga.mapper.AgentEventMapper">
    <select id="findAfter" resultType="com.manga.entity.AgentEvent">
        SELECT id, run_id, sequence_no, event_type, payload_json, created_at
        FROM manga_agent_event
        WHERE run_id = #{runId} AND sequence_no &gt; #{afterSequence}
        ORDER BY sequence_no ASC
    </select>
</mapper>
